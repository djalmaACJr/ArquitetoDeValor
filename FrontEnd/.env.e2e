# Variáveis injetadas pelo Vite quando rodando com --mode e2e
# (`npm run dev:e2e`). Habilita o modo de teste do storage do supabase:
# o auth fica em `localStorage` em vez de `sessionStorage`, para que o
# `storageState` do Playwright consiga persistir a sessão entre specs.
VITE_E2E=true

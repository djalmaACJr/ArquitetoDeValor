<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- external-files-path (pasta externa PRÓPRIA do app) em vez de
         external-path (raiz de todo o armazenamento externo compartilhado)
         — não há hoje nenhum plugin de câmera/compartilhamento instalado,
         isso só existe como boilerplate do @capacitor/android core. -->
    <external-files-path name="my_images" path="." />
    <cache-path name="my_cache_images" path="." />
</paths>